# Migration `20200528102836-password-user`

This migration has been generated by Mayck Coelho at 5/28/2020, 10:28:36 AM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
ALTER TABLE `mydb`.`Post` DROP FOREIGN KEY `Post_ibfk_1`;

ALTER TABLE `mydb`.`Profile` DROP FOREIGN KEY `Profile_ibfk_1`;

ALTER TABLE `mydb`.`User` ADD COLUMN `password` varchar(191) NOT NULL  ;

ALTER TABLE `mydb`.`Post` ADD FOREIGN KEY (`authorId`) REFERENCES `mydb`.`User`(`id`) ON DELETE CASCADE ON UPDATE CASCADE

ALTER TABLE `mydb`.`Profile` ADD FOREIGN KEY (`userId`) REFERENCES `mydb`.`User`(`id`) ON DELETE CASCADE ON UPDATE CASCADE

ALTER TABLE `mydb`.`Profile` RENAME INDEX `userId` TO `Profile.userId`

ALTER TABLE `mydb`.`User` RENAME INDEX `email` TO `User.email`
```

## Changes

```diff
diff --git schema.prisma schema.prisma
migration ..20200528102836-password-user
--- datamodel.dml
+++ datamodel.dml
@@ -1,0 +1,36 @@
+generator client {
+  provider = "prisma-client-js"
+}
+
+datasource db {
+  provider = "mysql"
+  url      = env("DATABASE_URL")
+}
+
+model Post {
+  authorId  Int
+  content   String?
+  createdAt DateTime @default(now())
+  id        Int      @default(autoincrement()) @id
+  published Boolean  @default(false)
+  title     String
+  User      User     @relation(fields: [authorId], references: [id])
+
+  @@index([authorId], name: "authorId")
+}
+
+model Profile {
+  bio    String?
+  id     Int     @default(autoincrement()) @id
+  userId Int     @unique
+  User   User    @relation(fields: [userId], references: [id])
+}
+
+model User {
+  email    String   @unique
+  id       Int      @default(autoincrement()) @id
+  name     String?
+  password String
+  Post     Post[]
+  Profile  Profile?
+}
```


